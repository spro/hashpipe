Using to build agent pipelines especially over JSON structured outputs and/or tool calls - each individual agent being a process even that could be called with some arguments as command line inputs or the prompt etc, use hash pipe only for the orchestration and having things be parallelized / sequenced. Essentially a command line version of a node based editor using the syntax of hash pipe 

Maybe the more interesting part of that would be a generalized cli api for calling the agents 

What are some actual interesting workflows that could be done with that? For example what does the WTWISF demo look like with 